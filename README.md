Prerequires:
* nvim 0.5+
* (optional)ccls

### Backup old config
```bash
mv ~/.config/nvim ~/.config/nvim-backup
```

## Install config
```bash
git clone https://github.com/3lv/nvim ~/.config/nvim
nvim +PackerSync
```
